# Implementation Plans

Generated by the improve skill on 2026-07-11. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add gateway sportsbook route tests | P2 | M | — | DONE |
| 002 | Update all missed docs and config for the current project state | P3 | M | — | DONE |
| 003 | Fix sportsbook endpoint data quality and live event status | P1 | M | — | DONE |
| 004 | Production-readiness hardening — full endpoint verification and startup race | P1 | M | — | DONE |
| 005 | Sportsbook v1 — single bet placement and settlement | P1 | L | 006 | DONE |
| 006 | Add DebitWallet and CreditWallet gRPC RPCs to the wallet service | P0 | M | — | DONE |
| 007 | Fix sportsbook gRPC auth metadata and add sportsbook authorization interceptor | P0 | M | 006 | DONE |
| 008 | Harden sportsbook bet placement and settlement money safety | P1 | M | 007 | DONE |
| 009 | Update project docs to reflect sportsbook v1 | P2 | S | — | DONE |
| 010 | Address remaining sportsbook code-review findings | P1 | S | 009 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- Plan 005 (Sportsbook v1) depends on Plan 006 (wallet debit/credit RPCs).
- Plan 007 depends on Plan 006 because it assumes the wallet service already enforces privileged caller rules on `DebitWallet`/`CreditWallet`.
- Plan 008 depends on Plan 007 because the wallet debit/credit RPCs must be callable before testing the money-safety hardening.
- Plan 009 has no dependencies but should be done after Plan 007 and Plan 008 so the described APIs are stable.

## Reconciliation

- **2026-07-10**: Plan 001 verified at `HEAD` (`ea050d1`). All done criteria still hold:
  - `go test -race ./internal/gateway/...` ✅
  - `go test -race ./internal/oddsfeed/...` ✅
  - `go test -race ./...` ✅
  - `go build -o bin/gateway ./cmd/gateway` ✅
  - `go build -o bin/oddsfeed ./cmd/oddsfeed` ✅
  - `gofmt -d internal/gateway/server_test.go internal/oddsfeed/server.go` clean ✅

## Findings considered and rejected

- **Missing composite DB indexes** (`ListBets(user_id, created_at)`, `ListPendingBets(status, created_at)`, `ListTransactions(user_id, created_at)`): LOW-confidence performance win; defer until load testing shows a problem rather than speculatively adding indexes.
- **Frontend go-api-client missing sportsbook methods**: valid feature gap, but user-selected priority was docs + blockers + money safety. Schedule as a follow-up direction plan after Plans 007–009.
- **No CI/CD pipeline**: valid DX gap, but out of scope for the selected findings. Recommended as a separate DX plan after the immediate security/correctness work.
